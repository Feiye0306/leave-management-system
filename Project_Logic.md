# 系統完整功能與業務邏輯說明書 (Project Business Logic)

## 1. 特休假 (Annual Leave) 邏輯

### 1.1 計算規則
特休假依據《勞動基準法》及員工「到職日 (Hire Date)」進行精準計算。系統自動根據年資計算法定應得天數：

| 年資條件 | 特休天數 |
| :--- | :--- |
| 滿半年 (6 個月) | 3 天 |
| 滿 1 年 | 7 天 |
| 滿 2 年 | 10 天 |
| 滿 3 年 | 14 天 |
| 滿 5 年 | 15 天 |
| 滿 10 年以上 | 每滿 1 年加 1 天 (上限 30 天) |

### 1.2 年度重置機制 (`AnnualResetService`)
- **觸發時機**：每次進入系統或重新整理頁面時，守護服務自動巡檢所有在職員工。
- **重置邏輯**：
  - 當「今日日期」為員工之「到職週年日 (Anniversary)」，且該年度尚未重置過時觸發。
  - **已休天數 (`used`)**：歸零。
  - **應得天數 (`entitlement`)**：依新的一年年資重新計算。
  - **到期日 (`expiryDate`)**：推算至下一個週年日前一天。
  - **稽核紀錄**：自動寫入一筆 `system_reset_annual` 操作日誌，避免重複執行。

---

## 2. 排休 (Personal Leave) 邏輯

### 2.1 定義與累計機制
- **定義**：除特休外的常態休假（含補休、調休、輪休等），統稱為「排休」。
- **額度累計 (`MonthlyAccrualService`)**：
  - 每位員工具備「每月排休限額 (`monthlyPersonalQuota`)」（預設 4 天）。
  - 每月 1 號系統自動將該額度加入「已獲得 (`earned`)」天數中，支援跨月遞延。
- **負數餘額 (Debit)**：
  - 允許員工預支或緊急超休，餘額顯示為負數，系統不暴力中斷作業，保留行政調配彈性。

### 2.2 時段劃分與鎖定規則
- **全天 (Full Day)**：扣除 1.0 天。
- **時段 (Time Slot)**：
  - 分為「上午 (Morning)」、「下午 (Afternoon)」、「晚上 (Evening)」三時段。
  - 每個時段各扣除 **0.5 天**。
  - **三選二互斥鎖定**：同一天選滿任 2 個時段時，第 3 個時段自動鎖定 (Disabled)，杜絕單日超選。

---

## 3. 自訂名目假期調整與特殊排假 (`CustomLeaveModal`)

### 3.1 功能說明
- **進入入口**：排休申請頁面頂部按鈕「⚡ 自訂名目增減 / 特殊排假」。
- **事由名目**：支援任意手動輸入，或點擊快捷標籤：
  - 颱風假補償、加班補休、值班津貼折抵、專案獎勵、事假扣除、生理假折抵、特殊排休。
- **調整幅度**：提供 `+1`、`+0.5`、`+0.25`、`-0.25`、`-0.5`、`-1` 天及任意自訂小數。

### 3.2 雙模式處理邏輯
1. **不指定日期（純額度增減）**：
   - 數值直接累加至該員工的 `adjustment` 欄位（影響特休或排休之可用餘額）。
   - 公式：`可用餘額 = 初始 + 已得 + 調整 - 已用`。
   - 不佔用日曆格子，直接寫入稽核日誌。
2. **指定日期（登記排假）**：
   - 選擇特定日期並確認送出。
   - 系統在日曆該日期建立包含 `customTitle`（如「颱風假補償 0.5天」）的排休卡片。
   - 自動將該天數扣減員工相應之休假額度，並即時反映於日曆與統計面板。

---

## 4. 排休更正、移動與刪除機制

- 點擊日曆中任何已排休的格子，即彈出「排休管理與更正彈窗」：
  - **移動日期**：可將原排休直接改選移至另一天。
  - **更動內容**：可更改假別、自訂名目、時段或天數。
  - **刪除假單**：點擊「刪除」將自動移除該筆排休紀錄，並**即時退還對應扣除之額度**。

---

## 5. 營運儀表板、報表與稽核模組

### 5.1 儀表板 (Dashboard)
- 在職人數統計、今日請假名單、當月壽星即時提醒。
- 同一校區同一天 2 人以上排休之衝突黃色預警提示。

### 5.2 報表中心 (Report Center)
- **分校月度排休總表 (PDF)**：橫向排版、內嵌繁體中文字型，直接下載無亂碼。
- **個人年度差勤明細 (PDF)**：單一員工全年度休假歷史匯總。
- **60 天到期警示名單**：自動清查特休即將逾期人員。
- **全校餘額結算表 (Balance Sheet)**：一表掌握全員特休/排休之初始、已用、調整與剩餘。

### 5.3 不可篡改稽核日誌 (Audit Log)
- 記錄所有請假申請、刪除更正、額度手動微調、系統重置與資料匯出。
- 嚴格記錄異動前數值 (`before`) 與異動後數值 (`after`)。
