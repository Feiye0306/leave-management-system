# 系統完整功能與邏輯說明書

## 1. 特休假 (Annual Leave) 邏輯

### 1.1 計算規則
特休假依據《勞動基準法》及員工「到職日」進行計算。系統會自動根據員工的年資計算應得天數。

| 年資條件 | 特休天數 |
| :--- | :--- |
| 滿半年 (6個月) | 3 天 |
| 滿 1 年 | 7 天 |
| 滿 2 年 | 10 天 |
| 滿 3 年 | 14 天 |
| 滿 5 年 | 15 天 |
| 滿 10 年以上 | 每一年加 1 天 (上限 30 天) |

### 1.2 年度重置機制
*   **觸發時機**：
    *   系統會在每次啟動或重新整理時 (`AnnualResetService`)，檢查所有在職員工。
    *   比對「今天日期」與員工的「到職日週年 (Hire Date Anniversary)」。
*   **重置邏輯**：
    *   若今天剛好是員工的到職週年日 (例如：2020/05/20 入職，每年 05/20 重置)，且今年尚未執行過重置。
    *   **已休天數 (Used)**：歸零 (但不清空 `adjustment` 手動調整值，視需求而定，目前邏輯保留歷史調整)。
    *   **應得天數 (Entitlement)**：依據新的一年年資重新計算。
    *   **有效期限 (Expiry)**：推算至下一個週年日的前一天。
*   **記錄**：
    *   每次重置都會在「操作日誌」中寫入一筆 `system_reset_annual` 紀錄，確保不會重複執行。

---

## 2. 排休 (Personal Leave) 邏輯

### 2.1 定義與累計機制 (New!)
*   **定義**：除了特休之外的休假，通常包含事假、病假或補休等，統稱為「排休」。
*   **額度與累計 (Accumulation)**：
    *   每位員工設定「每月排休限額 (Monthly Personal Quota)」。
    *   **累計制度**：每月 1 號，系統 (`MonthlyAccrualService`) 會自動將「每月額度」加入員工的「已獲得 (Earned)」天數中。
    *   **負數餘額**：排休餘額允許為負數 (Debit)，表示預支或超休。

### 2.2 申請限制與檢查
在提交排休申請 (`LeaveInputV2`) 時，系統執行以下檢查：

1.  **衝突檢查 (Conflict Check)**：
    *   檢查同一分校、同一日期，是否已有其他員工排休。
    *   若同一天已有 **2 人以上** 排休，系統會彈出警告，但允許強制提交。

2.  **額度檢查 (Quota Check)**：
    *   檢查當月排休總數是否超過 `monthlyPersonalQuota` (僅作提示，不強制阻擋，因支援累計/負數)。

### 2.3 扣除與時段規則
*   **全天 (Full Day)**：扣除 1.0 天。
*   **時段 (Time Slot)**：
    *   系統提供三個時段：上午、下午、晚上。
    *   **鎖定機制 (Locking)**：使用者在同一天最多只能選擇 **2 個時段**。當選滿 2 個時段後，第 3 個時段會自動鎖定 (Disabled) 無法選擇。
    *   扣除計算：每個時段扣除 **0.5 天** (選 2 個時段 = 1.0 天)。

---

## 3. 手動調整 (Manual Adjustment)

### 3.1 功能說明
*   位置：員工管理 (Employee List) > 編輯 (Edit) > 調整額度 (Adjust Quota)。
*   **單位**：支援 **0.25 天** 為單位的增減調整 (例如 +0.25, -0.5, +1.0)。
*   **原因紀錄**：調整時必須輸入「調整原因 (Reason)」。
*   **邏輯**：調整數值會累加至 `adjustment` 欄位，直接影響「可用餘額 (Available Balance)」。
    *   公式：`Available = Initial + Earned + Adjustment - Used`
*   **日誌**：所有手動調整皆會記錄於「操作日誌」，類別為 `manual_adjustment`。

---

## 4. 系統其他功能模組

### 4.1 儀表板 (Dashboard)
*   **即時狀態**：在職人數、今日排休名單。
*   **版面配置**：採三欄式設計，移除生日卡片，優化資訊呈現密度。
*   **排休總覽日曆**：以月曆形式查看全分校狀況，橘色為特休，藍色為排休。

### 4.2 報表中心 (Report Center)
*   **月度公告 (Monthly Report)**：分校月度排休總表 (Landscape)。
*   **個人明細 (Individual Report)**：特定員工年度休假紀錄與餘額詳情。
*   **到期警示 (Expiry Report)**：60天內特休即將到期名單。
*   **餘額結算 (Balance Sheet)** (New!)：全校員工特休與排休的詳細餘額表 (包含初始、已用、調整、剩餘)。
*   **餘額結算 (Balance Sheet)** (New!)：全校員工特休與排休的詳細餘額表 (包含初始、已用、調整、剩餘)。

### 4.3 資料備份 (Data Backup)
*   匯出/還原 JSON 格式的完整系統資料。

### 4.4 操作日誌 (Audit Log)
*   完整記錄：登入、請假(批次)、手動調整、系統重置、月度累計等操作。
